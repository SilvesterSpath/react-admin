import { Card, CardContent, Typography, Box } from "@mui/material";

const Homepage = () => {
  return (
    <Box>
      <Card>
        <CardContent>
          <Typography variant="h5">Welcome to the administration</Typography>
        </CardContent>
      </Card>
    </Box>
  );
};

export default Homepage;
